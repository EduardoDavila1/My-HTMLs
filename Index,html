<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dev Portfolio | Proyectos</title>
    <style>
        :root {
            --bg-color: #0f1115;
            --card-bg: #161b22;
            --text-main: #e6edf3;
            --text-dim: #8b949e;
            --accent: #58a6ff;
            --accent-glow: rgba(88, 166, 255, 0.15);
            --border: #30363d;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; }
        
        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            padding: 40px 20px;
        }

        header {
            text-align: center;
            margin-bottom: 50px;
            animation: fadeInDown 0.8s ease;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 10px;
            background: linear-gradient(90deg, #fff, #a5d6ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p.subtitle {
            color: var(--text-dim);
            font-size: 1.1rem;
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
            width: 100%;
            max-width: 1200px;
            animation: fadeInUp 1s ease;
        }

        .card {
            background-color: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 25px;
            text-decoration: none;
            color: var(--text-main);
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: var(--accent);
            box-shadow: 0 10px 30px -10px var(--accent-glow);
        }

        .card h3 {
            font-size: 1.2rem;
            margin-bottom: 8px;
        }

        .card span.type {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-dim);
            background: rgba(255,255,255,0.05);
            padding: 4px 8px;
            border-radius: 4px;
            align-self: flex-start;
            margin-bottom: 15px;
        }

        .card .icon {
            font-size: 1.5rem;
            margin-bottom: 15px;
            opacity: 0.8;
        }

        .status-dot {
            height: 8px;
            width: 8px;
            background-color: var(--accent);
            border-radius: 50%;
            position: absolute;
            top: 20px;
            right: 20px;
            box-shadow: 0 0 10px var(--accent);
        }

        /* Animations */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

    <header>
        <h1>Mis Proyectos</h1>
        <p class="subtitle">Selecciona un archivo para visualizar</p>
    </header>

    <div class="grid-container">
        <a href="Alpha Check.html" class="card">
            <span class="type">App</span>
            <div class="icon">🚀</div>
            <h3>Alpha Check</h3>
            <div class="status-dot"></div>
        </a>

        <a href="Antena.html" class="card">
            <span class="type">Tool</span>
            <div class="icon">📡</div>
            <h3>Antena</h3>
        </a>

        <a href="Cazando la nieve.html" class="card">
            <span class="type">Game / Visual</span>
            <div class="icon">❄️</div>
            <h3>Cazando la Nieve</h3>
        </a>

        <a href="EdDoc.HTML" class="card">
            <span class="type">Document</span>
            <div class="icon">📝</div>
            <h3>EdDoc</h3>
        </a>

        <a href="ffmpg.html" class="card">
            <span class="type">Utility</span>
            <div class="icon">🎬</div>
            <h3>FFMPEG Tool</h3>
        </a>
        
        <a href="formato.html" class="card">
            <span class="type">Utility</span>
            <div class="icon">📐</div>
            <h3>Formato</h3>
        </a>

        <a href="mision nieve.html" class="card">
            <span class="type">Game</span>
            <div class="icon">🏔️</div>
            <h3>Misión Nieve</h3>
        </a>

        <a href="retiro de invierno.html" class="card">
            <span class="type">Visual</span>
            <div class="icon">🔥</div>
            <h3>Retiro de Invierno</h3>
        </a>

        <a href="seguridad.html" class="card">
            <span class="type">System</span>
            <div class="icon">🔒</div>
            <h3>Seguridad</h3>
        </a>

        <a href="SharkTank.html" class="card">
            <span class="type">Pitch</span>
            <div class="icon">🦈</div>
            <h3>Shark Tank</h3>
        </a>
    </div>

</body>
</html>
